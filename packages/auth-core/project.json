{
  "name": "auth-core",
  "sourceRoot": "packages/auth-core/src",
  "tags": ["package"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "tsup", "cwd": "packages/auth-core" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit", "cwd": "packages/auth-core" }
    }
  }
}
