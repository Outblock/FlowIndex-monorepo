{
  "name": "flow-ui",
  "sourceRoot": "packages/flow-ui/src",
  "tags": ["package"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "tsup", "cwd": "packages/flow-ui" }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc --noEmit", "cwd": "packages/flow-ui" }
    }
  }
}
